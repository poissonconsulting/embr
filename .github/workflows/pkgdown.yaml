name: pkgdown
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  release:
    types: [published]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  pkgdown:
    uses: poissonconsulting/.github/.github/workflows/pkgdown.yaml@v1
    with:
      cmdstan: true
      private: false
    secrets: inherit
