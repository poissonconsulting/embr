% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse.R
\name{analyse}
\alias{analyse}
\alias{analyse.mb_model}
\title{Analyse Models}
\usage{
analyse(x, ...)

\method{analyse}{mb_model}(
  x,
  data,
  nchains = getOption("mb.nchains", 3L),
  niters = getOption("mb.niters", 1000L),
  nthin = getOption("mb.thin", NULL),
  parallel = getOption("mb.parallel", FALSE),
  quiet = getOption("mb.quiet", TRUE),
  glance = getOption("mb.glance", TRUE),
  beep = getOption("mb.beep", TRUE),
  seed = sample.int(.Machine$integer.max, 1),
  stan_engine = getOption("mb.stan_engine", character(0)),
  niters_warmup = niters,
  ...
)
}
\arguments{
\item{x}{An \code{mb_model} object.}

\item{...}{Additional arguments passed to the underlying estimation
function.}

\item{data}{A data frame, or a named list of data frames for meta-analysis
across datasets.}

\item{nchains}{A count of the number of chains (default: 3).}

\item{niters}{A count of the number of iterations to save per chain (default: 1000).}

\item{nthin}{A count of the thinning interval.}

\item{parallel}{A flag indicating whether to perform the analysis in parallel if possible.}

\item{quiet}{A flag indicating whether to disable messages and warnings, including sampling progress.}

\item{glance}{A flag indicating whether to print a model summary.}

\item{beep}{A flag indicating whether to beep on completion of the analysis.}

\item{seed}{A positive whole number specifying the seed to use. The default is random. This is currently only implemented for Stan models.}

\item{stan_engine}{A string selecting the Stan engine:
\itemize{
\item \code{"cmdstan-mcmc"}: MCMC via \code{\link[cmdstanr:model-method-sample]{cmdstanr::sample()}}.
\item \code{"cmdstan-pathfinder"}: pathfinder via \code{\link[cmdstanr:model-method-pathfinder]{cmdstanr::pathfinder()}}.
\item \code{"cmdstan-variational"}: variational ADVI via \code{\link[cmdstanr:model-method-variational]{cmdstanr::variational()}}.
\item \code{"cmdstan-optimize"}: optimization via \code{\link[cmdstanr:model-method-optimize]{cmdstanr::optimize()}}.
\item \code{"cmdstan-laplace"}: Laplace approximation via \code{\link[cmdstanr:model-method-laplace]{cmdstanr::laplace()}}.
}

Defaults to \code{character(0)}. Any value other than the five above (including
the empty default) falls back to MCMC via \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}}. Ignored for
JAGS models, which always use \link[rjags:rjags-package]{rjags}.}

\item{niters_warmup}{A count of the number of warmup iterations. The default is to use the same number of iterations as \code{niters}. This is currently only implemented for Stan models.}
}
\value{
An \code{mb_analysis} if \code{data} is a single data frame, or an
\code{mb_meta_analysis} if \code{data} is a named list of data frames.
}
\description{
Fit a Bayesian hierarchical model to data using Stan (via rstan or cmdstanr)
or JAGS (via rjags). The estimation backend is determined by the model
class (JAGS or Stan) and, for Stan models, by the \code{stan_engine} argument.
}
\details{
The following applies to Stan models using a \verb{cmdstan-*} \code{stan_engine}. For
each engine, embr sets a fixed set of cmdstanr arguments from its own
parameters (\code{niters}, \code{nchains}, \code{nthin}, \code{seed}, etc.). These
embr-controlled arguments, listed in each engine section below, cannot be
overridden via \code{...}; any such arguments passed are dropped with a warning.
All other cmdstanr arguments can be passed through \code{...}; see the linked
reference for the full set.

\code{init} is the sole exception: it can be overridden via \code{...}, replacing
inits generated by \code{gen_inits()} in \code{\link[=model]{model()}}. Non-MCMC engines do not use
\code{gen_inits()}.

\code{nthin} and \code{niters_warmup} are ignored by non-MCMC engines. For
pathfinder, variational, and laplace, \code{niters} sets the number of draws
from the approximated posterior.
}
\section{cmdstan-mcmc}{

Calls \code{\link[cmdstanr:model-method-sample]{cmdstanr::sample()}}.

embr-controlled: \code{chains}, \code{parallel_chains}, \code{iter_warmup},
\code{iter_sampling}, \code{thin}, \code{data}, \code{seed}, \code{init}, \code{show_messages},
\code{show_exceptions}.

Common pass-throughs: \code{adapt_delta}, \code{max_treedepth}, \code{step_size},
\code{refresh}, \code{output_dir}, \code{save_warmup}.
}

\section{cmdstan-pathfinder}{

Calls \code{\link[cmdstanr:model-method-pathfinder]{cmdstanr::pathfinder()}}.

embr-controlled: \code{data}, \code{seed}, \code{init}, \code{draws} (from \code{niters}),
\code{show_messages}, \code{show_exceptions}.

Common pass-throughs: \code{num_paths}, \code{history_size}, \code{max_lbfgs_iters},
\code{psis_resample}, \code{tol_rel_grad}.
}

\section{cmdstan-variational}{

Calls \code{\link[cmdstanr:model-method-variational]{cmdstanr::variational()}}.

embr-controlled: \code{data}, \code{seed}, \code{init}, \code{draws} (from \code{niters}),
\code{show_messages}, \code{show_exceptions}.

Common pass-throughs: \code{algorithm} (\code{"meanfield"} or \code{"fullrank"}), \code{iter},
\code{tol_rel_obj}, \code{eval_elbo}, \code{eta}.
}

\section{cmdstan-optimize}{

Calls \code{\link[cmdstanr:model-method-optimize]{cmdstanr::optimize()}}.

embr-controlled: \code{data}, \code{seed}, \code{init}, \code{show_messages},
\code{show_exceptions}.

Common pass-throughs: \code{algorithm} (\code{"lbfgs"}, \code{"bfgs"}, or \code{"newton"}),
\code{iter}, \code{tol_obj}, \code{history_size}, \code{jacobian}.
}

\section{cmdstan-laplace}{

Calls \code{\link[cmdstanr:model-method-laplace]{cmdstanr::laplace()}}.

embr-controlled: \code{data}, \code{seed}, \code{init}, \code{mode}, \code{draws} (from \code{niters}),
\code{show_messages}, \code{show_exceptions}.

Common pass-throughs: \code{jacobian}, \code{opt_args}.
}

\section{rstan (default Stan backend)}{

For Stan models without a \code{stan_engine} matching one of the cmdstan
options above, \code{\link[rstan:stanmodel-method-sampling]{rstan::sampling()}} is called. Pass its arguments through
\code{...}; for example, \code{control = list(adapt_delta = 0.95)}.
}

\examples{
\dontrun{
# Stan model with rstan (default)
analysis <- analyse(stan_model, data, nchains = 4, niters = 1000)

# Stan model with cmdstanr MCMC
analysis <- analyse(stan_model, data,
  stan_engine = "cmdstan-mcmc",
  nchains = 4, niters = 1000
)

# Stan model with cmdstanr pathfinder
analysis <- analyse(stan_model, data,
  stan_engine = "cmdstan-pathfinder",
  niters = 500
)

# JAGS model
analysis <- analyse(jags_model, data, nchains = 4, niters = 2000)

# Passing engine-specific arguments through ...
analysis <- analyse(stan_model, data,
  stan_engine = "cmdstan-mcmc",
  adapt_delta = 0.99,
  iter_warmup = 500L
)
analysis <- analyse(stan_model, data,
  control = list(adapt_delta = 0.95) # rstan::sampling argument
)

# Overriding initial values for cmdstanr MCMC
init_fun <- function() list(bIntercept = 0)
analysis <- analyse(stan_model, data,
  stan_engine = "cmdstan-mcmc",
  init = init_fun
)

# Multiple datasets
analyses <- analyse(model, list(dataset1 = data1, dataset2 = data2))
}

}
\seealso{
\itemize{
\item The \href{https://poissonconsulting.github.io/embr/articles/analyse.html}{analyse article}
for a walkthrough of the fitting workflow.
\item \code{\link[=model]{model()}} to build a model.
\item \code{\link[=analyse.mb_models]{analyse.mb_models()}} to fit a list of models simultaneously.
\item \code{\link[=analyse.character]{analyse.character()}} to fit from raw model code.
\item \code{\link[=predict.mb_analysis]{predict.mb_analysis()}} and \code{\link[=coef.mb_analysis]{coef.mb_analysis()}} to summarise an
analysis.
\item \code{\link[=reanalyse]{reanalyse()}} to refit with different sampling settings.
}
}
